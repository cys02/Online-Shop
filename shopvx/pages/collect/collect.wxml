<view class="father">
    <view  class="down-sp">
      <view catchtap="detail" data-item="{{item}}" class="down-item" wx:for="{{collectProducts}}" wx:key="item"><image class="logo" mode="aspectFit"  src="{{api}}{{item.logo}}"></image>
        <view>{{item.product}}</view>
        <view>￥{{item.price}}</view>
        <view>库存：{{item.num}}</view>
      </view>
    </view>
</view>